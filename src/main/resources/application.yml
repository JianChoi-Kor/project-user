spring:
  output:
    ansi:
      enabled: always
  datasource:
    url: jdbc:h2:mem:test
    driver-class-name: org.h2.Driver
    name: sa
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    hibernate:
      ddl-auto: create